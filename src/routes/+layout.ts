// Enable prerendering for all pages by default
// This is required for static site generation with adapter-static
export const prerender = true;

// Enable SPA mode for client-side routing
export const ssr = true;
